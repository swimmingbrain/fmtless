# fmtless

Logging for C that leaves the words at home.

`printf` sends the text of every log line and formats it on the device.
fmtless sends only the values, and puts the line back together on a host that
has the firmware's ELF.

Work in progress.

## License

MIT.
